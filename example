import logging
logging.basicConfig(filename='frnn.log', level=logging.DEBUG)
from plasma.conf_parser import parse_config
from plasma.preprocessor.preprocess import Preprocessor

conf = parse_config("conf_test_preprocessor.yaml")


shot_list_dir = conf['paths']['shot_list_dir']
shot_files = conf['paths']['shot_files']
shot_files_test = conf['paths']['shot_files_test']
train_frac = conf['training']['train_frac']
stateful = conf['model']['stateful']

shot_list_path = conf["paths"]["saved_shot_list_path"]

np.random.seed(0)
random.seed(0)

# Construct config object
my_conf = parse_config("conf_test_preprocessor.yaml")

shot_list_train, shot_list_validate, shot_list_test = guarantee_preprocessed(my_conf)

